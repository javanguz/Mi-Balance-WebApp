<%- include('layout/_header') %>
<style>.pin-form-wrapper { max-width: 220px; margin: 0 auto; }</style>
<h1 class="mb-4 text-center"><%= title %></h1>
<p class="text-center text-muted">Esta es su última oportunidad para cancelar. Para confirmar la eliminación <strong>definitiva e irreversible</strong> de todos los datos, ingrese su PIN de acceso actual.</p>
<div class="card">
    <div class="card-body">
        <% if (error) { %><div class="alert alert-danger pin-form-wrapper"><%= error %></div><% } %>
        <div class="pin-form-wrapper">
            <form action="/settings/database/delete" method="POST">
                <div class="mb-3"><label for="pin" class="form-label visually-hidden">PIN Actual</label><input type="password" name="pin" class="form-control form-control-lg text-center" id="pin" required maxlength="4" pattern="\d{4}" autofocus></div>
                <div class="d-grid gap-2"><button type="submit" class="btn btn-danger">Confirmar Eliminación Definitiva</button><a href="/inicio" class="btn btn-outline-primary">Cancelar</a></div>
            </form>
        </div>
    </div>
</div>
<%- include('layout/_footer') %>